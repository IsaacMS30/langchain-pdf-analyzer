# PDF Analyzer – RAG with LangChain, Groq y Chroma

This project implements a **RAG (Retrieval-Augmented Generation)** system that allows to upload PDF file, divide them into chunks, store them in a vectorstore (Chroma) and make consults using **Groq**.

This project uses:

- **LangChain**
- **Groq API**
- **ChromaDB** as vectorstore
